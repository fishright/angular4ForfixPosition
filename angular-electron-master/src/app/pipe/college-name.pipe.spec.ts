import { CollegeNamePipe } from './college-name.pipe';

describe('CollegeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CollegeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
