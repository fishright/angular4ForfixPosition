import { CourseToPlacePipe } from './course-to-place.pipe';

describe('CourseToPlacePipe', () => {
  it('create an instance', () => {
    const pipe = new CourseToPlacePipe();
    expect(pipe).toBeTruthy();
  });
});
