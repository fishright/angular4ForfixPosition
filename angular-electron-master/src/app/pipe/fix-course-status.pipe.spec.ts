import { FixCourseStatusPipe } from './fix-course-status.pipe';

describe('FixCourseStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new FixCourseStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
