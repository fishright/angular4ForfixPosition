import { FixStatusPipe } from './fix-status.pipe';

describe('FixStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new FixStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
